Create a `.env` file in the `frontend-new` directory by copying `.env.example`.

Example:

EXPO_PUBLIC_API_URL=https://your-ngrok-url.ngrok-free.app

Replace the URL with the ngrok URL generated after running:

npx ngrok http 5000
